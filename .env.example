# Supabase (Vite) environment variables
# Copy this file to `.env` and replace values with your project credentials.

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Optional: if you want to use a service role key for server-side scripts,
# keep it out of the browser and use serverless functions.
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
